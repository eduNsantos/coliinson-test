info:
  name: Get rank
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/ranking?search=São Paulo"
  params:
    - name: search
      value: São Paulo
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
